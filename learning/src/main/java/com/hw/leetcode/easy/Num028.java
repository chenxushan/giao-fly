package com.hw.leetcode.easy;

public class Num028 {
    public int strStr(String haystack, String needle) {
        if (haystack.isEmpty() || needle==null || needle.isEmpty()) return 0;
        int p,q;
        for (int i =0; i < haystack.length();i++) {

        }
        return 0;
    }
}
